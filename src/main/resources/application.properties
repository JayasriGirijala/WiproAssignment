# === Application Info ===
spring.application.name=SpringAssignmentWipro

# === Server Config ===
server.port=8080

# === Database Configuration ===
spring.datasource.url=jdbc:mysql://localhost:3306/grocery_db?useSSL=false&serverTimezone=UTC

spring.datasource.username=root
spring.datasource.password=Jayasri11@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === Hibernate / JPA ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# === Thymeleaf ===
spring.thymeleaf.cache=false

# === Message Source for Validation ===
spring.messages.basename=ValidationMessages

 